# Artifact templates

Initialize a proportional subset with `python scripts/new_design_project.py --slug <name> --mode <mode>`. Templates are decision scaffolds, not paperwork. Delete irrelevant sections, mark status, and never populate evidence fields with invented content.
