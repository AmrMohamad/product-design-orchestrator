# Product Design Orchestrator agent plugin 0.4.1

This generated directory is a self-contained plugin for Claude Code and Codex. It contains both official manifest formats, a flat `skills/` directory with 70 skills, and all governance/resources required after marketplace caching or direct installation.

Start with:

- Claude Code: `/product-design-orchestrator:pdo-start`
- Codex plugin: mention `pdo-start` or select it from skills
- Codex direct installation: `$pdo-start`

Regenerate from the package root with `python scripts/build_agent_bundle.py`.
