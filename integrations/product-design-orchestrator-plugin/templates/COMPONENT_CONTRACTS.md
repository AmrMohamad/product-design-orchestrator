# Component contracts — {{PROJECT_SLUG}}

## Component: [name]

- User/system purpose; why this is reusable:
- Semantic/native primitive:
- API/slots/data ownership:
- Variants vs states (do not confuse them):
- Complete state model: default, hover where relevant, focus-visible, active, selected, checked, loading, disabled, read-only, invalid, success, empty:
- Keyboard, pointer, touch, screen-reader behavior:
- Accessible name/description/error relationships:
- Content length/localization/RTL:
- Responsive/container behavior:
- Tokens and theming:
- Motion/reduced motion:
- Performance and SSR/hydration constraints:
- Component, interaction, accessibility, visual tests:
- Usage, non-usage, migration examples:
